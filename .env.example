OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
PORT=3001

# HTTP Basic Auth for the API server — required, the server refuses to start
# without both set. Generate a strong password, e.g. `openssl rand -base64 18`.
AUTH_USER=
AUTH_PASSWORD=
